spring.application.name=PDNight

spring.datasource.url=jdbc:mysql://${DB_HOST}:3306/PDNight
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=create-drop

spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.highlight_sql=true

logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


logging.config=classpath:logback.xml
logging.level.org.springframework.data.elasticsearch.client.WIRE= TRACE

#spring.data.redis.repositories.enabled=false

# ==============================
# Redis
# ==============================
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}

# ?? ? ?? (Lettuce ?? ? ???)
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0
spring.data.redis.lettuce.pool.max-wait=5000



# Actuator endpoints ???
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# Prometheus? ??? export ??
management.prometheus.metrics.export.enabled=true

# ?? ?? ??
management.endpoints.web.base-path=/actuator
management.prometheus.metrics.export.step=1s

spring.freemarker.enabled=true
spring.freemarker.template-loader-path=classpath:/template/
spring.freemarker.suffix=.ftl
spring.freemarker.charset=UTF-8
spring.mvc.view.prefix=
spring.mvc.view.suffix=
jwt.secret.key=64uI64ukIOuPmeyCrOuvuOyngOuzuOqzteycvO2DnOuwqOyKpOqxsCDqtawg7J6l6rCc64+Z

#kafka
spring.kafka.bootstrap-servers= 127.0.0.1:10000,127.0.0.1:10001,127.0.0.1:10002

# Oauth
oauth2.google.client-id=${GOOGLE_CLIENT_ID}
oauth2.google.client-secret=${GOOGLE_CLIENT_SECRET}
oauth2.google.redirect-uri=http://localhost:8080/oauth/callback/google
oauth2.google.token-endpoint=https://oauth2.googleapis.com/token
oauth2.google.user-info-endpoint=https://www.googleapis.com/oauth2/v3/userinfo
oauth2.google.authorization-endpoint=https://accounts.google.com/o/oauth2/v2/auth